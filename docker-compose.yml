version: '3.8'
services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_APP=vehicles
      - FLASK_ENV=production
    volumes:
      - .:/app:ro
    restart: unless-stopped
